use dotenv::dotenv;
use std::env;


fn petfinder() {
    let client: Client = Client::new();
    let mut pet: Vec<Value> = Vec::new();
    let mut page_token: String = String::new();

    loop {
        let url: String = format{
            //need to format url for api access
        };
    }

}